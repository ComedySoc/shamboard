<?xml version="1.0" encoding="UTF-8"?>

<?import de.jensd.fx.glyphs.fontawesome.*?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import org.controlsfx.control.textfield.CustomTextField?>
<BorderPane stylesheets="/shamboard/style.css" xmlns="http://javafx.com/javafx/8.0.121"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="shamboard.Controller">
    <center>
        <GridPane fx:id="buttonGrid" alignment="center" hgap="10" style="-fx-background-color: pink" vgap="10">
            <ButtonBar fx:id="btnbarTop">
                <buttons>
                    <Button fx:id="btnSting" onAction="#playSound">
                        <graphic>
                            <FontAwesomeIconView glyphName="BOLT" styleClass="mainButtonGlyphs"/>
                        </graphic>
                    </Button>
                    <Button fx:id="btnHouseMusic" onAction="#playSound">
                        <graphic>
                            <FontAwesomeIconView glyphName="HOME" styleClass="mainButtonGlyphs"/>
                        </graphic>
                    </Button>
                    <Button fx:id="btnRunOn" onAction="#playSound">
                        <graphic>
                            <FontAwesomeIconView glyphName="SIGN_IN" styleClass="mainButtonGlyphs"/>
                        </graphic>
                    </Button>
                    <Button fx:id="btnRunOff" onAction="#playRunOffMusic">
                        <graphic>
                            <FontAwesomeIconView glyphName="SIGN_OUT" styleClass="mainButtonGlyphs"/>
                        </graphic>
                    </Button>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                </buttons>
                <GridPane.margin>
                    <Insets bottom="30.0"/>
                </GridPane.margin>
            </ButtonBar>

            <ButtonBar styleClass="launchpadButtons" GridPane.rowIndex="1">
                <buttons>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                </buttons>
            </ButtonBar>

            <ButtonBar styleClass="launchpadButtons" GridPane.rowIndex="2">
                <buttons>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                </buttons>
            </ButtonBar>

            <ButtonBar styleClass="launchpadButtons" GridPane.rowIndex="3">
                <buttons>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                </buttons>
            </ButtonBar>

            <ButtonBar styleClass="launchpadButtons" GridPane.rowIndex="4">
                <buttons>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                </buttons>
            </ButtonBar>

            <ButtonBar styleClass="launchpadButtons" GridPane.rowIndex="5">
                <buttons>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                </buttons>
            </ButtonBar>

            <ButtonBar styleClass="launchpadButtons" GridPane.rowIndex="6">
                <buttons>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                </buttons>
            </ButtonBar>

            <ButtonBar styleClass="launchpadButtons" GridPane.rowIndex="7">
                <buttons>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                </buttons>
            </ButtonBar>

            <ButtonBar styleClass="launchpadButtons" GridPane.rowIndex="8">
                <buttons>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                    <Button onAction="#playSound"/>
                </buttons>
            </ButtonBar>

            <VBox fx:id="vboxRightButtons" alignment="CENTER" spacing="10" GridPane.columnIndex="1"
                  GridPane.rowIndex="1" GridPane.rowSpan="8">
                <Button onAction="#playSound"/>
                <Button onAction="#playSound"/>
                <Button onAction="#playSound"/>
                <Button onAction="#playSound"/>
                <Button onAction="#playSound"/>
                <Button onAction="#playSound"/>
                <Button onAction="#playSound"/>
                <Button onAction="#playSound"/>
                <GridPane.margin>
                    <Insets left="30.0"/>
                </GridPane.margin>
            </VBox>
            <columnConstraints>
                <ColumnConstraints/>
                <ColumnConstraints/>
            </columnConstraints>
            <rowConstraints>
                <RowConstraints/>
                <RowConstraints/>
                <RowConstraints/>
                <RowConstraints/>
                <RowConstraints/>
                <RowConstraints/>
                <RowConstraints/>
                <RowConstraints/>
                <RowConstraints/>
            </rowConstraints>
        </GridPane>
    </center>
    <top>
        <Label fx:id="lblNowPlaying" alignment="CENTER" text="Now Playing"/>
    </top>

    <right>
        <VBox>
            <ToolBar fx:id="barSearch">
                <items>
                    <CustomTextField fx:id="fieldSearch" prefWidth="230.0" styleClass="searchField">
                        <left>
                            <Label fx:id="lblSearch">
                                <graphic>
                                    <FontAwesomeIconView glyphName="SEARCH"/>
                                </graphic>
                            </Label>
                        </left>
                    </CustomTextField>
                    <Label fx:id="labelMatches"/>
                </items>
            </ToolBar>

            <ListView prefWidth="248.0" VBox.vgrow="ALWAYS"/>
        </VBox>
    </right>
</BorderPane>
